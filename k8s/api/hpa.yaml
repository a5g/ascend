apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ascend-api-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ascend-api-blue # CI updates this during rollout
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
