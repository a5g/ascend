1. Add Tailwind CSS and PostCSS config to `@ascend/mfe-position-sizing` package. Update `webpack.config.js` to process CSS.
2. Update `PositionSizing.tsx` using HTML semantics from the references in `ui/*/code.html`, implementing the layout logic with inputs changing dynamically depending on the selected strategy. Add "For informational purposes only" disclaimer. Implement Save Config panel and logic using tailwind UI.
3. Pre-commit check steps.
4. Submit change.
