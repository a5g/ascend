{
  "name": "@ascend/root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "echo 'No tests specified yet'",
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @ascend/mfe-alerts --filter @ascend/mfe-super-admin --filter @ascend/mfe-user-management --filter @ascend/web --parallel run dev"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "protobufjs"
    ],
    "overrides": {
      "serialize-javascript": "^7.0.5"
    }
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
