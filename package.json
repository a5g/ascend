{
  "name": "@ascend/root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "echo 'No tests specified yet'",
    "build": "pnpm -r run build",
    "db:migrate": "pnpm --filter @ascend/db exec sequelize-cli db:migrate"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "protobufjs"
    ],
    "overrides": {
      "serialize-javascript": "^7.0.5"
    }
  }
}
